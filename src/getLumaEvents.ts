
// go get the luma events FROM the browser, fml.
// use puppeteer or something

import type { LumaFullEventType } from "./lumaTypes"

// return the luma events
export const getLumaEvents = (): LumaFullEventType[] => {
    return []
}